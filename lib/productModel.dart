import 'package:flutter/material.dart';

class ProductModel {
  String principalController;
  String _currentItemSelected;
  String _selectedCreditLoad;

  productModel(String course, String grade, String load){
    this.principalController = principalController;
    this._currentItemSelected = _currentItemSelected;
    this._selectedCreditLoad  = _selectedCreditLoad;
  }
}